# ============================================
# Server Configuration
# ============================================
PORT=8787
HOST=0.0.0.0
MOCK_MODE=false
PUBLIC_BASE_URL=http://localhost:8787

# ============================================
# AMap (高德地图) Web Service
# ============================================
# 高德地图 Web 服务 API Key
# 获取地址：https://console.amap.com/dev/key/app
AMAP_WEB_SERVICE_KEY=your_amap_web_service_key

# ============================================
# AI Services (AI 服务)
# ============================================
# 豆包 (Doubao) API Key - 用于图片/视频生成
# 获取地址：https://console.volcengine.com/
ARK_API_KEY=your_ark_api_key

# DeepSeek API Key - 用于文本生成/翻译/情感分析
# 获取地址：https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key

# ============================================
# Database Configuration (数据库配置)
# ============================================
# 方式 1：使用 DATABASE_URL (推荐用于 Railway)
# DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# 方式 2：使用单独的环境变量 (推荐用于本地开发)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_postgres_password
PGDATABASE=boundless_lens

# PostgreSQL SSL 模式 (Railway 需要)
# PGSSLMODE=require

# 强制 UTF-8 编码 (支持中文)
PGCLIENTENCODING=UTF8

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件为 .env.server (本地开发) 或 .env (生产环境)
# 2. 填入实际的 API 密钥和数据库密码
# 3. .env.server 和 .env 已在 .gitignore 中，不会被提交到 Git
# 4. Railway 部署时使用 DATABASE_URL，本地开发使用 PG* 变量

# ============================================
# 阿里云 OSS 配置（图片存储）
# ============================================
# 阿里云 OSS 用于永久存储海报图片
# 获取地址：https://oss.console.aliyun.com/
ALIYUN_OSS_REGION=oss-ap-southeast-1
ALIYUN_OSS_ACCESS_KEY_ID=your_aliyun_access_key_id
ALIYUN_OSS_ACCESS_KEY_SECRET=your_aliyun_access_key_secret
ALIYUN_OSS_BUCKET=your_bucket_name

# ============================================
# Railway 部署配置
# ============================================
# 在 Railway 中需要配置以下环境变量：
# - PORT (自动设置)
# - HOST=0.0.0.0
# - DATABASE_URL (从 PostgreSQL 插件自动获取)
# - ALIYUN_OSS_REGION
# - ALIYUN_OSS_ACCESS_KEY_ID
# - ALIYUN_OSS_ACCESS_KEY_SECRET
# - ALIYUN_OSS_BUCKET
# - AMAP_WEB_SERVICE_KEY
# - ARK_API_KEY
# - DEEPSEEK_API_KEY
# - PUBLIC_BASE_URL=https://your-service.up.railway.app

# ============================================
# 注意事项
# ============================================
# ⚠️ 不要将包含真实密钥的文件提交到 Git
# ⚠️ 本地开发使用 .env.server
# ⚠️ 生产环境在 Railway 中配置环境变量
# ⚠️ DATABASE_URL 优先级高于单独的 PG* 变量
