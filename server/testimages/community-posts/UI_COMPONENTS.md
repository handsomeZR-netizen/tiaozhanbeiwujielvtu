# 社区灵感 UI 组件设计

## 整体布局策略

### 瀑布流布局（Masonry Layout）
- 双列自适应布局
- 卡片高度根据内容自动调整
- 响应式：移动端单列，平板/桌面双列

### 信息密度优化
- 缩略图比例：4:5 或 3:4（竖版优先）
- 减少大留白，紧凑排列
- 关键信息前置（标题、用户、位置）

---

## 核心组件设计

### 1. PostCard（帖子卡片）

**基础结构**：
```
┌─────────────────────┐
│   [图片 4:5]        │
│   └─ [标签]         │
├─────────────────────┤
│ 标题（2行截断）      │
│ 用户 + 国旗 + 时间   │
│ 位置 | 💬 评论 ❤️ 赞 │
├─────────────────────┤
│ [文化提示 badge]    │
└─────────────────────┘
```

**交互状态**：
- Hover: 轻微上浮 + 阴影加深
- 点击: 展开详情模态框
- 图片加载: Shimmer 骨架屏

**关键样式**：
```css
.post-card {
  border-radius: 4px;
  overflow: hidden;
  transition: all 0.3s ease;
  background: white;
  border: 1px solid #e5e5e5;
}

.post-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.1);
}

.post-card-image {
  aspect-ratio: 4/5;
  object-fit: cover;
  filter: sepia(0.05); /* 轻微复古感 */
}
```

---

### 2. UserBadge（用户标识）

**结构**：
```
[头像] 艾玛 🇩🇪 · 2026/01/15
```

**设计要点**：
- 头像：圆形，16x16px
- 国旗 emoji：表示国籍
- 时间戳：灰色小字
- 字体：10-11px

---

### 3. TagChip（标签芯片）

**样式**：
```
#胡同  #北京  #慢生活
```

**设计要点**：
- 小尺寸：9-10px 字体
- 半透明背景：`bg-black/60 backdrop-blur`
- 圆角：2px
- 最多显示 2 个标签，其余用 `+2` 表示

---

### 4. CultureTip（文化提示）

**结构**：
```
💡 胡同是北京传统居住区，保留了老北京的生活方式
```

**设计要点**：
- 浅色背景：`bg-amber-50`
- 边框：`border-l-4 border-amber-400`
- 图标：💡 或 ℹ️
- 可折叠/展开

---

### 5. FilterBar（筛选栏）

**结构**：
```
[#城市漫步] [#文化观察] [#手作日记] [#摄影日记] ...
```

**交互**：
- 横向滚动（隐藏滚动条）
- 选中状态：红色边框 + 浅红背景
- 支持多选

**样式**：
```css
.filter-chip {
  padding: 8px 16px;
  border: 1px solid #d4d4d4;
  border-radius: 2px;
  font-size: 14px;
  white-space: nowrap;
  cursor: pointer;
  transition: all 0.2s;
}

.filter-chip.active {
  border-color: #dc2626;
  background: #fef2f2;
  color: #dc2626;
}
```

---

### 6. PostDetailModal（帖子详情模态框）

**结构**：
```
┌─────────────────────────────┐
│ [关闭 X]                     │
│                              │
│ [大图 16:9]                  │
│                              │
│ 标题                         │
│ 用户 + 国旗 + 时间戳         │
│ 位置 | 花费                  │
│                              │
│ 正文内容（完整）             │
│                              │
│ 💡 文化提示                  │
│                              │
│ [标签列表]                   │
│                              │
│ ❤️ 234  💬 18               │
└─────────────────────────────┘
```

**交互**：
- 点击背景关闭
- ESC 键关闭
- 滚动查看完整内容
- 支持左右切换上/下一篇

---

## 动画细节

### 1. 卡片进入动画
```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.post-card {
  animation: fadeInUp 0.4s ease-out;
  animation-fill-mode: both;
}

/* 瀑布流逐个延迟 */
.post-card:nth-child(1) { animation-delay: 0.05s; }
.post-card:nth-child(2) { animation-delay: 0.1s; }
.post-card:nth-child(3) { animation-delay: 0.15s; }
```

### 2. 点赞动画
```css
@keyframes heartBeat {
  0%, 100% { transform: scale(1); }
  25% { transform: scale(1.2); }
  50% { transform: scale(0.95); }
}

.like-button.active {
  animation: heartBeat 0.3s ease;
  color: #dc2626;
}
```

### 3. 图片加载骨架屏
```css
@keyframes shimmer {
  0% { background-position: -1000px 0; }
  100% { background-position: 1000px 0; }
}

.skeleton {
  background: linear-gradient(
    90deg,
    #f0f0f0 25%,
    #e0e0e0 50%,
    #f0f0f0 75%
  );
  background-size: 1000px 100%;
  animation: shimmer 2s infinite;
}
```

---

## 响应式设计

### 移动端（< 640px）
- 单列布局
- 卡片宽度 100%
- 标签横向滚动
- 简化用户信息显示

### 平板（640px - 1024px）
- 双列布局
- 卡片间距 16px
- 完整信息显示

### 桌面（> 1024px）
- 双列布局（可选三列）
- 卡片间距 20px
- Hover 效果增强

---

## 真实感设计细节

### 1. 字体选择
- 标题：`font-serif`（宋体/衬线体）
- 正文：`font-sans`（无衬线体）
- 数字/时间：`font-mono`（等宽字体）

### 2. 色彩方案
- 主色：`#dc2626`（印章红）
- 背景：`#fafaf9`（纸张白）
- 文字：`#1c1917`（墨色）
- 辅助：`#78716c`（灰色）

### 3. 纹理与质感
- 背景纹理：`bg-texture`（纸张纹理）
- 卡片阴影：`shadow-paper`（纸张投影）
- 图片滤镜：`sepia(0.05)`（轻微复古）

### 4. 微交互
- 按钮 Hover：轻微缩放 + 颜色变化
- 卡片 Hover：上浮 2px + 阴影加深
- 标签点击：涟漪效果
- 图片加载：渐进式显示

---

## 实现优先级

### P0（核心功能）
- [x] PostCard 基础组件
- [x] 瀑布流布局
- [x] 标签筛选
- [x] 基础交互（点赞/评论）

### P1（体验优化）
- [ ] PostDetailModal 详情模态框
- [ ] 图片懒加载
- [ ] 骨架屏加载
- [ ] 动画效果

### P2（高级功能）
- [ ] 无限滚动加载
- [ ] 用户关注系统
- [ ] 评论功能
- [ ] 分享功能

---

## 技术栈建议

- **布局**: CSS Grid / Flexbox
- **瀑布流**: `react-masonry-css` 或自定义实现
- **动画**: Framer Motion 或 CSS Animations
- **图片**: Next/Image 或 lazy loading
- **状态管理**: React Hooks（useState/useReducer）

---

## 参考资源

- 小红书：瀑布流布局、卡片设计
- Instagram：图片展示、交互动画
- Pinterest：标签筛选、无限滚动
- Behance：文化内容展示

---

**创建时间**: 2026/01/23  
**版本**: v1.0
